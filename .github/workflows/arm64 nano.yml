name: Build static nano for Android (arm64) - latest version

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y make wget curl

      - name: Download Android NDK
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip android-ndk-r25c-linux.zip
          echo "NDK_PATH=${PWD}/android-ndk-r25c" >> $GITHUB_ENV

      - name: Set up NDK environment
        run: |
          export TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android
          export API=21
          export BIN_DIR=$TOOLCHAIN/bin

          echo "$BIN_DIR" >> $GITHUB_PATH

          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "API=$API" >> $GITHUB_ENV
          echo "CC=$BIN_DIR/clang -target $TARGET$API" >> $GITHUB_ENV
          echo "CXX=$BIN_DIR/clang++ -target $TARGET$API" >> $GITHUB_ENV
          echo "AR=$BIN_DIR/llvm-ar" >> $GITHUB_ENV
          echo "RANLIB=$BIN_DIR/llvm-ranlib" >> $GITHUB_ENV
          echo "STRIP=$BIN_DIR/llvm-strip" >> $GITHUB_ENV

      - name: Test compiler and linker
        run: |
          echo "=== Testing compiler and linker ==="
          cat > test.c << 'EOF'
          #include <stdio.h>
          int main() {
              printf("Hello\n");
              return 0;
          }
          EOF

          echo "--- Compiling to object file ---"
          $CC -c test.c -o test.o 2>&1 | tee compile.log
          if [ ! -f test.o ]; then
            echo "Compilation failed. Full output:"
            cat compile.log
            exit 1
          fi
          echo "Compilation successful."

          echo "--- Linking object file ---"
          $CC test.o -o test 2>&1 | tee link.log
          if [ ! -f test ]; then
            echo "Linking failed. Full output:"
            cat link.log
            exit 1
          fi
          echo "Linking successful."
          echo "Compiler and linker test passed."

      - name: Get latest ncurses version
        id: ncurses
        run: |
          LATEST=$(curl -s ftp://ftp.gnu.org/gnu/ncurses/ | grep -oP 'ncurses-\K[0-9]+\.[0-9]+' | sort -V | tail -n1)
          if [ -z "$LATEST" ]; then
            LATEST="6.5"
          fi
          echo "version=$LATEST"
          echo "version=$LATEST" >> $GITHUB_OUTPUT
          echo "url=ftp://ftp.gnu.org/gnu/ncurses/ncurses-$LATEST.tar.gz" >> $GITHUB_OUTPUT

      - name: Get latest nano version
        id: nano
        run: |
          HTML=$(curl -s https://www.nano-editor.org/download.php)
          LATEST=$(echo "$HTML" | grep -oP 'nano-\K[0-9]+\.[0-9]+(?=\.tar\.xz)' | head -1)
          if [ -z "$LATEST" ]; then
            MAJOR=$(curl -s https://www.nano-editor.org/dist/ | grep -oP 'v\K[0-9]+' | sort -n | tail -1)
            if [ -n "$MAJOR" ]; then
              LATEST=$(curl -s https://www.nano-editor.org/dist/v$MAJOR/ | grep -oP "nano-$MAJOR\.[0-9]+\.tar\.xz" | grep -oP "nano-\K[0-9]+\.[0-9]+" | sort -V | tail -1)
            fi
          fi
          if [ -z "$LATEST" ]; then
            LATEST="8.3"
          fi
          echo "version=$LATEST"
          echo "version=$LATEST" >> $GITHUB_OUTPUT
          echo "url=https://www.nano-editor.org/dist/v${LATEST%%.*}/nano-$LATEST.tar.xz" >> $GITHUB_OUTPUT

      - name: Download and extract ncurses
        run: |
          wget "${{ steps.ncurses.outputs.url }}"
          tar xf ncurses-*.tar.gz
          SRC_DIR=$(tar tf ncurses-*.tar.gz | head -1 | cut -d/ -f1)
          echo "NCURSES_SRC_DIR=$PWD/$SRC_DIR" >> $GITHUB_ENV

      - name: Build ncurses (static)
        run: |
          cd $NCURSES_SRC_DIR
          make distclean || true
          ./configure \
            --host=$TARGET \
            --prefix=$PWD/install \
            --without-shared \
            --with-normal \
            --without-debug \
            --without-ada \
            --enable-widec \
            --enable-termcap \
            --with-terminfo-dirs=/system/etc/terminfo:/data/misc/terminfo \
            --with-default-terminfo-dir=/system/etc/terminfo \
            CFLAGS="-fPIE"
          make -j$(nproc)
          make install.libs install.data
          echo "NCURSES_DIR=$PWD/install" >> $GITHUB_ENV

      - name: Debug and detect ncurses library
        run: |
          echo "=== NCURSES_DIR/lib contents ==="
          ls -la $NCURSES_DIR/lib
          echo "=== Searching for ncurses static library ==="
          if [ -f "$NCURSES_DIR/lib/libncursesw.a" ]; then
            echo "Found libncursesw.a"
            echo "NCURSES_LIB_NAME=ncursesw" >> $GITHUB_ENV
          elif [ -f "$NCURSES_DIR/lib/libncurses.a" ]; then
            echo "Found libncurses.a"
            echo "NCURSES_LIB_NAME=ncurses" >> $GITHUB_ENV
          else
            echo "ERROR: No ncurses static library found!"
            exit 1
          fi

      - name: Locate terminfo directory
        id: locate_terminfo
        run: |
          echo "=== Searching for terminfo directory under $NCURSES_DIR ==="
          TERMINFO_PATH=$(find "$NCURSES_DIR" -type d -name terminfo | head -n1)
          if [ -z "$TERMINFO_PATH" ]; then
            echo "ERROR: Could not find terminfo directory"
            exit 1
          fi
          echo "Found terminfo at: $TERMINFO_PATH"
          echo "TERMINFO_PATH=$TERMINFO_PATH" >> $GITHUB_ENV

      - name: Download and extract nano
        run: |
          wget "${{ steps.nano.outputs.url }}"
          tar xf nano-*.tar.xz
          SRC_DIR=$(tar tf nano-*.tar.xz | head -1 | cut -d/ -f1)
          echo "NANO_SRC_DIR=$PWD/$SRC_DIR" >> $GITHUB_ENV

      - name: Configure nano
        run: |
          cd $NANO_SRC_DIR
          export CPPFLAGS="-I$NCURSES_DIR/include -I$NCURSES_DIR/include/ncursesw"
          export LDFLAGS="-L$NCURSES_DIR/lib"
          export CFLAGS="-fPIE -O2"
          ./configure \
            --host=$TARGET \
            --build=x86_64-linux-gnu \
            --target=$TARGET \
            --prefix=$PWD/install \
            --with-ncursesw \
            --enable-utf8 \
            --disable-nls \
            --disable-speller \
            ac_cv_func_setenv=yes

      - name: Compile nano (static)
        run: |
          cd $NANO_SRC_DIR
          echo "=== Compiling with library: -l${NCURSES_LIB_NAME} ==="
          echo "=== Library path: -L$NCURSES_DIR/lib ==="
          make -j$(nproc) LDFLAGS="-L$NCURSES_DIR/lib -static -pie" LIBS="-l${NCURSES_LIB_NAME}"
          make install

      - name: Strip and package
        run: |
          cd $NANO_SRC_DIR/install/bin
          $STRIP nano
          echo "=== Checking linked libraries (should only show system libs) ==="
          readelf -d nano | grep NEEDED || true

          # Create output directory
          mkdir -p $GITHUB_WORKSPACE/nano-android/terminfo

          # Copy terminfo from detected location
          echo "Copying terminfo from $TERMINFO_PATH"
          cp -r "$TERMINFO_PATH"/* $GITHUB_WORKSPACE/nano-android/terminfo/

          cp nano $GITHUB_WORKSPACE/nano-android/
          cd $GITHUB_WORKSPACE/nano-android
          tar -czf $GITHUB_WORKSPACE/nano-android-static.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nano-android-static-latest
          path: nano-android-static.tar.gz
