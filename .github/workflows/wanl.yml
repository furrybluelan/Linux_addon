      - name: Prepare Modules (Ultra Fast Mode)
        run: |
          cd kernel
          # 仅生成必要的头文件，不进行全局 HDRINST
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- LLVM=1 LLVM_IAS=1 \
               KCFLAGS="-Wno-error -Wno-implicit-function-declaration" \
               scripts prepare -j$(nproc)
